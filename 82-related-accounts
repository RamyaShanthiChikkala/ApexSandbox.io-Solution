public Set<Id> accountIds(List<Opportunity> opps) {

Set<Id> related = new Set<Id>();
 for(Opportunity op : opps)
 {
     if(op.AccountId != null || op.AccountId !== '')
     related.add(op.AccountId);
     
 }

 return related;
}