public Set<Id> accountIds(List<Opportunity> opps) {

Set<Id> relatedSet = new Set<Id>();
 for(Opportunity op : opps)
 {
     if(op.AccountId != null && op.AccountId != '' && op.AccountId.length() == 18)
     relatedSet.add(op.AccountId);
     
 }

 return relatedSet;
}