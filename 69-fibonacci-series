public List<Integer> fibonacciSeries(Integer n) {
    
Integer a = 0;

Integer b = 1;

Integer c = 0;
List<Integer> fib = new List<Integer>();
if(n<1)
return fib;
c = a+b;
fib.add(c);

for(Integer i = 2 ; i < = n ; i++)
{ 
    fib.add(c);
    a = b ;
    b = i;
    c = a + b;
}

return fib;
}