public static String reverseWords(String str){
 
 List<String> words = new List<String>();
 words = str.split(' ');

 for(Integer i = 0 ; i < words.size(); i++)
 {
     String rev = words[i].reverse();
     words.set(i, rev);
 }
  return String.join(words, ' ');

}