public static List<Integer> squareList(List<Integer> nums){
   Integer temp;
   
   
   for(Integer i = 0 ; i< nums.size();  i++)
   {
       nums.set(i,nums[i]*nums[i]);
   }
   
   nums.sort();
    
    return nums;
}