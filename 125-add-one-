public void plusOne(List<Integer> numbers) {
    
Integer carry = 1;

for(int i = numbers.size() - 1 ; i >= 0 ; i++)
{
    Integer sum = number.get(i) + carry;
    if(sum > 9)
    {
        carry = 1;
        numbers.set(i,0);
    }
    else
    {
        carry = 0;
        numbers.set(i, sum)
    }

}
 if (carry == 1) {
            numbers.add(0, 1);
        }

}