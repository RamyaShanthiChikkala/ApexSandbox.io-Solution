public String getAccountsInJSONFormat(List<Account> accounts){
  String jsonString = '';
  for(Account ac : accounts)
  {
       jsonString = jsonString + JSON.serialize(ac,true);
  }
  return jsonString;
}
