public List<Integer> evenNumbers(Integer n) {
    
    List<Integer> positive = new List<Integer>();
    for(Integer i = 1 ; i <= n ; i = i++)
    {
        Integer a = 2*i ;
        positive.add(a);
    }
    return positive;
}